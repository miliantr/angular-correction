#include "src/math/linal.h"
#include "src/ballistics/const.h"

using namespace LinearAlgebra;

int main()
{
    TVector temp(3);
    temp[0] = Cx[1];
    temp[1] = Cx[5];
    temp[2] = Cx[2];
    temp.print();
    return 0;
}
